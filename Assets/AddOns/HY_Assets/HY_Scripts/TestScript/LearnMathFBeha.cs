using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LearnMathFBeha : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        float f = Mathf.Sin(90);
        print(f);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
